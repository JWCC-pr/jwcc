/* eslint-disable no-useless-escape */
export const REGEX = {
  // ================================ 이메일 ================================
  EMAIL: /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/,

  // ================================ 비밀번호 ================================
  PASSWORD: /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[@$!%*?&#])[A-Za-z\d@$!%*?&#]{8,20}$/,
}
