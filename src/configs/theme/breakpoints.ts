/**
 * + `mobile`: `base` ~ `sm`
 * + `tablet`: `sm` ~ `md`
 * + `desktop`: `md` ~ `lg`
 * + `lg desktop`: `lg` ~
 */
export const breakpoints = {
  base: '0px',
  sm: '768px',
  md: '1280px',
  lg: '1760px',
}
