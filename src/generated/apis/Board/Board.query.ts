import {
  InfiniteData,
  useInfiniteQuery,
  useMutation,
  useQuery,
} from '@tanstack/react-query'

import { fetchExtended } from '@/configs/fetch/fetch-extend'

import {
  BoardErrorMessageType,
  CommonErrorType,
} from '../@types/data-contracts'
import {
  InfiniteQueryHookParams,
  MutationHookParams,
  Parameter,
  QueryHookParams,
  RequestFnReturn,
} from '../@types/react-query-type'
import { BoardApi } from './Board.api'

/**
 * !DO NOT EDIT THIS FILE
 *
 * 스크립트가 실행될때, 파일을 항상 새로 쓰기 때문에 파일 수정시 작성내용이 제거 될 수 있습니다.
 */

/**
 * tok-cli.config.ts 에서 설정된 instance 경로의 axios instace 가 적용된, api 의 instance 입니다.
 */
export const boardApi = new BoardApi({ customFetch: fetchExtended })

/**
 * query key 에 undefined 를 포함시키지 않기 위한 함수입니다.
 */
const isDefined = (v: unknown) => typeof v !== 'undefined'

/**
 * query-keys
 */
export const QUERY_KEY_BOARD_API = {
  LIST: (variables?: Parameter<typeof boardApi.boardList>) =>
    ['BOARD_LIST', variables].filter(isDefined),
  LIST_INFINITE: (variables?: Parameter<typeof boardApi.boardList>) =>
    ['BOARD_LIST_INFINITE', variables].filter(isDefined),
  CREATE: () => ['BOARD_CREATE'],
  RETRIEVE: (variables?: Parameter<typeof boardApi.boardRetrieve>) =>
    ['BOARD_RETRIEVE', variables].filter(isDefined),
  UPDATE: () => ['BOARD_UPDATE'],
  DESTROY: () => ['BOARD_DESTROY'],
}

/**
 * No description
 *
 * @tags board
 * @name BoardList
 * @summary 자유 게시글 목록 조회
 * @request GET:/v1/board/
 * @secure    */

export const useBoardListQuery = <
  TData = RequestFnReturn<typeof boardApi.boardList>,
>(
  params?: QueryHookParams<
    typeof boardApi.boardList,
    { error: CommonErrorType },
    TData
  >,
) => {
  const queryKey = QUERY_KEY_BOARD_API.LIST(params?.variables)
  return useQuery({
    queryKey,
    queryFn: () => boardApi.boardList(params?.variables),
    ...params?.options,
  })
}

/**
 * No description    *      * @tags board
 * @name BoardList
 * @summary 자유 게시글 목록 조회
 * @request GET:/v1/board/
 * @secure    */
export const useBoardListInfiniteQuery = <
  TData = InfiniteData<
    RequestFnReturn<typeof boardApi.boardList>,
    Parameter<typeof boardApi.boardList>
  >,
>(
  params?: InfiniteQueryHookParams<
    typeof boardApi.boardList,
    CommonErrorType,
    TData
  >,
) => {
  const queryKey = QUERY_KEY_BOARD_API.LIST_INFINITE(params?.variables)
  return useInfiniteQuery({
    queryKey,
    initialPageParam: null,
    queryFn: ({ pageParam }) => {
      const offset = pageParam ?? params?.variables?.query?.offset ?? 0
      return boardApi.boardList({
        ...params?.variables,
        query: { ...params?.variables?.query, offset },
      })
    },
    getNextPageParam: (lastPage, allPages) => {
      if (!lastPage?.isNext) return null
      const fetchedLength = allPages?.length || 0
      const initialOffset = params?.variables?.query?.offset || 0
      const limit = params?.variables?.query?.limit || 0

      return initialOffset + fetchedLength * limit
    },
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags board
 * @name BoardCreate
 * @summary 자유 게시글 등록
 * @request POST:/v1/board/
 * @secure  */

export const useBoardCreateMutation = (
  params: MutationHookParams<
    typeof boardApi.boardCreate,
    { error: BoardErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_BOARD_API.CREATE()
  return useMutation({
    mutationKey,
    mutationFn: boardApi.boardCreate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags board
 * @name BoardRetrieve
 * @summary 자유 게시글 상세 조회
 * @request GET:/v1/board/{id}/
 * @secure    */

export const useBoardRetrieveQuery = <
  TData = RequestFnReturn<typeof boardApi.boardRetrieve>,
>(
  params: QueryHookParams<
    typeof boardApi.boardRetrieve,
    { error: CommonErrorType },
    TData
  >,
) => {
  const queryKey = QUERY_KEY_BOARD_API.RETRIEVE(params.variables)
  return useQuery({
    queryKey,
    queryFn: () => boardApi.boardRetrieve(params.variables),
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags board
 * @name BoardUpdate
 * @summary 자유 게시글 수정
 * @request PUT:/v1/board/{id}/
 * @secure  */

export const useBoardUpdateMutation = (
  params: MutationHookParams<
    typeof boardApi.boardUpdate,
    { error: BoardErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_BOARD_API.UPDATE()
  return useMutation({
    mutationKey,
    mutationFn: boardApi.boardUpdate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags board
 * @name BoardDestroy
 * @summary 자유 게시글 삭제
 * @request DELETE:/v1/board/{id}/
 * @secure  */

export const useBoardDestroyMutation = (
  params: MutationHookParams<
    typeof boardApi.boardDestroy,
    { error: CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_BOARD_API.DESTROY()
  return useMutation({
    mutationKey,
    mutationFn: boardApi.boardDestroy,
    ...params?.options,
  })
}
