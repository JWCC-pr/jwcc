import { useMutation, useQuery } from '@tanstack/react-query'

import { fetchExtended } from '@/configs/fetch/fetch-extend'

import {
  CommonErrorType,
  RepeatRoomReservationErrorMessageType,
} from '../@types/data-contracts'
import {
  MutationHookParams,
  Parameter,
  QueryHookParams,
  RequestFnReturn,
} from '../@types/react-query-type'
import { RepeatRoomReservationApi } from './RepeatRoomReservation.api'

/**
 * !DO NOT EDIT THIS FILE
 *
 * 스크립트가 실행될때, 파일을 항상 새로 쓰기 때문에 파일 수정시 작성내용이 제거 될 수 있습니다.
 */

/**
 * tok-cli.config.ts 에서 설정된 instance 경로의 axios instace 가 적용된, api 의 instance 입니다.
 */
export const repeatRoomReservationApi = new RepeatRoomReservationApi({
  customFetch: fetchExtended,
})

/**
 * query key 에 undefined 를 포함시키지 않기 위한 함수입니다.
 */
const isDefined = (v: unknown) => typeof v !== 'undefined'

/**
 * query-keys
 */
export const QUERY_KEY_REPEAT_ROOM_RESERVATION_API = {
  CREATE: () => ['REPEAT_ROOM_RESERVATION_CREATE'],
  RETRIEVE: (
    variables?: Parameter<
      typeof repeatRoomReservationApi.repeatRoomReservationRetrieve
    >,
  ) => ['REPEAT_ROOM_RESERVATION_RETRIEVE', variables].filter(isDefined),
  UPDATE: () => ['REPEAT_ROOM_RESERVATION_UPDATE'],
  DESTROY: () => ['REPEAT_ROOM_RESERVATION_DESTROY'],
}

/**
 * @description `repeatType=weekly` 사용 시 `weekdays`는 0(월)~6(일) 숫자 배열입니다. `weekOfMonth`는 1~4주차 지정값 배열이며 복수 선택 가능합니다(예: [1, 3]). 매주 반복이면 `[]` 또는 생략합니다. `repeatType=monthlyDate` 사용 시 `monthDay`를 1~31로 전달합니다.
 *
 * @tags repeat_room_reservation
 * @name RepeatRoomReservationCreate
 * @summary 교리실 반복 예약 등록
 * @request POST:/v1/repeat_room_reservation/
 * @secure  */

export const useRepeatRoomReservationCreateMutation = (
  params: MutationHookParams<
    typeof repeatRoomReservationApi.repeatRoomReservationCreate,
    { error: RepeatRoomReservationErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_REPEAT_ROOM_RESERVATION_API.CREATE()
  return useMutation({
    mutationKey,
    mutationFn: repeatRoomReservationApi.repeatRoomReservationCreate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags repeat_room_reservation
 * @name RepeatRoomReservationRetrieve
 * @request GET:/v1/repeat_room_reservation/{id}/
 * @secure    */

export const useRepeatRoomReservationRetrieveQuery = <
  TData = RequestFnReturn<
    typeof repeatRoomReservationApi.repeatRoomReservationRetrieve
  >,
>(
  params: QueryHookParams<
    typeof repeatRoomReservationApi.repeatRoomReservationRetrieve,
    { error: CommonErrorType },
    TData
  >,
) => {
  const queryKey = QUERY_KEY_REPEAT_ROOM_RESERVATION_API.RETRIEVE(
    params.variables,
  )
  return useQuery({
    queryKey,
    queryFn: () =>
      repeatRoomReservationApi.repeatRoomReservationRetrieve(params.variables),
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags repeat_room_reservation
 * @name RepeatRoomReservationUpdate
 * @summary 교리실 반복 예약 수정
 * @request PUT:/v1/repeat_room_reservation/{id}/
 * @secure  */

export const useRepeatRoomReservationUpdateMutation = (
  params: MutationHookParams<
    typeof repeatRoomReservationApi.repeatRoomReservationUpdate,
    { error: RepeatRoomReservationErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_REPEAT_ROOM_RESERVATION_API.UPDATE()
  return useMutation({
    mutationKey,
    mutationFn: repeatRoomReservationApi.repeatRoomReservationUpdate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags repeat_room_reservation
 * @name RepeatRoomReservationDestroy
 * @summary 교리실 반복 예약 삭제
 * @request DELETE:/v1/repeat_room_reservation/{id}/
 * @secure  */

export const useRepeatRoomReservationDestroyMutation = (
  params: MutationHookParams<
    typeof repeatRoomReservationApi.repeatRoomReservationDestroy,
    { error: CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_REPEAT_ROOM_RESERVATION_API.DESTROY()
  return useMutation({
    mutationKey,
    mutationFn: repeatRoomReservationApi.repeatRoomReservationDestroy,
    ...params?.options,
  })
}
