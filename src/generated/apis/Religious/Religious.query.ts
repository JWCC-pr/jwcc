import { useMutation, useQuery } from '@tanstack/react-query'

import { fetchExtended } from '@/configs/fetch/fetch-extend'

import {
  CommonErrorType,
  ReligiousErrorMessageType,
} from '../@types/data-contracts'
import {
  MutationHookParams,
  Parameter,
  QueryHookParams,
  RequestFnReturn,
} from '../@types/react-query-type'
import { ReligiousApi } from './Religious.api'

/**
 * !DO NOT EDIT THIS FILE
 *
 * 스크립트가 실행될때, 파일을 항상 새로 쓰기 때문에 파일 수정시 작성내용이 제거 될 수 있습니다.
 */

/**
 * tok-cli.config.ts 에서 설정된 instance 경로의 axios instace 가 적용된, api 의 instance 입니다.
 */
export const religiousApi = new ReligiousApi({ customFetch: fetchExtended })

/**
 * query key 에 undefined 를 포함시키지 않기 위한 함수입니다.
 */
const isDefined = (v: unknown) => typeof v !== 'undefined'

/**
 * query-keys
 */
export const QUERY_KEY_RELIGIOUS_API = {
  LIST: (variables?: Parameter<typeof religiousApi.religiousList>) =>
    ['RELIGIOUS_LIST', variables].filter(isDefined),
  CREATE: () => ['RELIGIOUS_CREATE'],
  RETRIEVE: (variables?: Parameter<typeof religiousApi.religiousRetrieve>) =>
    ['RELIGIOUS_RETRIEVE', variables].filter(isDefined),
  UPDATE: () => ['RELIGIOUS_UPDATE'],
  DESTROY: () => ['RELIGIOUS_DESTROY'],
}

/**
 * No description
 *
 * @tags religious
 * @name ReligiousList
 * @summary 수도자 목록 조회 (재임 중)
 * @request GET:/v1/religious/
 * @secure    */

export const useReligiousListQuery = <
  TData = RequestFnReturn<typeof religiousApi.religiousList>,
>(
  params?: QueryHookParams<
    typeof religiousApi.religiousList,
    { error: CommonErrorType },
    TData
  >,
) => {
  const queryKey = QUERY_KEY_RELIGIOUS_API.LIST(params?.variables)
  return useQuery({
    queryKey,
    queryFn: () => religiousApi.religiousList(params?.variables),
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags religious
 * @name ReligiousCreate
 * @summary 수도자 등록
 * @request POST:/v1/religious/
 * @secure  */

export const useReligiousCreateMutation = (
  params: MutationHookParams<
    typeof religiousApi.religiousCreate,
    { error: ReligiousErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_RELIGIOUS_API.CREATE()
  return useMutation({
    mutationKey,
    mutationFn: religiousApi.religiousCreate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags religious
 * @name ReligiousRetrieve
 * @request GET:/v1/religious/{id}/
 * @secure    */

export const useReligiousRetrieveQuery = <
  TData = RequestFnReturn<typeof religiousApi.religiousRetrieve>,
>(
  params: QueryHookParams<
    typeof religiousApi.religiousRetrieve,
    { error: CommonErrorType },
    TData
  >,
) => {
  const queryKey = QUERY_KEY_RELIGIOUS_API.RETRIEVE(params.variables)
  return useQuery({
    queryKey,
    queryFn: () => religiousApi.religiousRetrieve(params.variables),
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags religious
 * @name ReligiousUpdate
 * @summary 수도자 수정
 * @request PUT:/v1/religious/{id}/
 * @secure  */

export const useReligiousUpdateMutation = (
  params: MutationHookParams<
    typeof religiousApi.religiousUpdate,
    { error: ReligiousErrorMessageType | CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_RELIGIOUS_API.UPDATE()
  return useMutation({
    mutationKey,
    mutationFn: religiousApi.religiousUpdate,
    ...params?.options,
  })
}

/**
 * No description
 *
 * @tags religious
 * @name ReligiousDestroy
 * @summary 수도자 삭제
 * @request DELETE:/v1/religious/{id}/
 * @secure  */

export const useReligiousDestroyMutation = (
  params: MutationHookParams<
    typeof religiousApi.religiousDestroy,
    { error: CommonErrorType }
  >,
) => {
  const mutationKey = QUERY_KEY_RELIGIOUS_API.DESTROY()
  return useMutation({
    mutationKey,
    mutationFn: religiousApi.religiousDestroy,
    ...params?.options,
  })
}
